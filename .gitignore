
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Individual project repos (each is its own git repo)
projects/*/

# Claude Code session files
.claude/scheduled_tasks.lock
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
